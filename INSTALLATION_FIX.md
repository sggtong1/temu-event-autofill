# 🔧 插件安装问题修复指南

## ❌ 问题描述
- 上传失败
- 无法读取插件版本号

## ✅ 解决方案

### 1. 检查文件完整性
确保以下文件都存在且内容正确：

```
D:\chrome_plugin\
├── manifest.json          ✅ 已修复
├── popup.html            ✅ 存在
├── popup.js              ✅ 存在
├── popup.css             ✅ 存在
├── content.js            ✅ 存在
├── content.css           ✅ 存在
└── background.js         ✅ 存在
```

### 2. 重新加载插件

#### 方法1：通过Chrome扩展管理页面
1. 打开Chrome浏览器
2. 访问 `chrome://extensions/`
3. 开启右上角的"开发者模式"
4. 点击"加载已解压的扩展程序"
5. 选择 `D:\chrome_plugin` 文件夹
6. 点击"选择文件夹"

#### 方法2：重新加载现有插件
1. 在 `chrome://extensions/` 页面
2. 找到"Temu活动价格自动填充工具"
3. 点击"重新加载"按钮（🔄）

### 3. 验证安装成功

#### 检查扩展状态
- ✅ 扩展名称：Temu活动价格自动填充工具
- ✅ 版本：1.0.0
- ✅ 状态：已启用
- ✅ 权限：已授予

#### 测试功能
1. 访问Temu活动页面
2. 点击浏览器工具栏中的插件图标
3. 应该看到popup窗口打开

### 4. 常见问题排查

#### 问题1：manifest.json格式错误
**症状**：无法读取插件版本号
**解决**：已修复，manifest.json现在包含正确的版本号

#### 问题2：文件路径问题
**症状**：找不到文件
**解决**：确保所有文件都在 `D:\chrome_plugin` 目录下

#### 问题3：权限问题
**症状**：插件无法访问页面
**解决**：检查manifest.json中的host_permissions

### 5. 手动验证步骤

#### 步骤1：检查manifest.json
```json
{
  "manifest_version": 3,
  "name": "Temu活动价格自动填充工具",
  "version": "1.0.0",
  "description": "自动填充Temu活动页面的价格信息，支持预设价格数据管理"
}
```

#### 步骤2：检查文件权限
确保Chrome有权限访问文件夹：
- 右键点击 `D:\chrome_plugin` 文件夹
- 选择"属性" → "安全"
- 确保当前用户有"完全控制"权限

#### 步骤3：清除缓存
1. 关闭Chrome浏览器
2. 重新打开Chrome
3. 重新加载插件

### 6. 成功安装后的验证

#### 插件图标
- 浏览器工具栏应该出现插件图标
- 点击图标应该打开popup窗口

#### Popup窗口功能
- ✅ 标题显示：💰 Temu价格填充工具
- ✅ 填充按钮：🚀 填充活动价格
- ✅ 缩放按钮：⛶（右上角）
- ✅ 数据管理标签页

#### 控制台日志
打开开发者工具（F12），在控制台应该看到：
```
TemuPriceFiller 初始化开始
TemuPriceFiller 初始化完成
```

## 🎯 如果仍然有问题

### 检查Chrome版本
- 确保Chrome版本 >= 88（支持Manifest V3）
- 更新到最新版本Chrome

### 检查扩展权限
1. 在 `chrome://extensions/` 页面
2. 点击插件详情
3. 确保所有权限都已授予

### 重新创建插件
如果问题持续存在：
1. 删除现有插件
2. 重新选择 `D:\chrome_plugin` 文件夹
3. 重新加载

## 📞 技术支持

如果按照以上步骤仍然无法解决问题，请提供：
1. Chrome版本号
2. 错误信息截图
3. 控制台错误日志
4. 操作系统版本

插件现在应该可以正常安装和使用了！

