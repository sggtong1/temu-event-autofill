# 优化后的插件使用指南

## 🎯 插件功能

### 核心功能
- **一键填充**: 点击按钮即可自动填充所有匹配的价格
- **智能匹配**: 根据货号精确匹配产品并填充对应价格
- **数据管理**: 支持添加、编辑、删除、导入导出价格数据

### 简化特性
- ✅ 移除了延时填充功能 - 填充操作即时完成
- ✅ 移除了填充通知 - 操作更简洁
- ✅ 只保留按钮事件触发 - 用户完全控制填充时机

## 📋 使用步骤

### 1. 设置价格数据
1. 点击浏览器工具栏中的插件图标
2. 在"数据管理"标签页中：
   - 点击"+ 添加价格"按钮添加单个价格
   - 或使用"导入导出"标签页批量导入数据

### 2. 在Temu页面使用
1. 访问Temu活动页面：
   `https://agentseller.temu.com/activity/marketing-activity/detail-new`
2. 页面加载完成后，会显示"💰 自动填充活动价格"按钮
3. 点击按钮，系统自动填充所有匹配的价格

### 3. 数据格式
支持以下数据格式：

**JSON格式**:
```json
[
  {
    "itemNumber": "92-BK-15",
    "activityPrice": 48.24,
    "costPrice": 30.00,
    "note": "便携式水冷风扇-黑色"
  }
]
```

**CSV格式**:
```csv
货号,活动价格,成本价格,备注
92-BK-15,48.24,30.00,便携式水冷风扇-黑色
```

## 🔧 技术优化

### 性能优化
- 移除了不必要的延时等待
- 简化了事件处理逻辑
- 减少了DOM操作

### 用户体验优化
- 移除了干扰性的通知弹窗
- 简化了设置界面
- 保留了核心功能

### 代码优化
- 移除了未使用的设置相关代码
- 简化了初始化流程
- 减少了存储操作

## 📊 功能对比

| 功能 | 优化前 | 优化后 |
|------|--------|--------|
| 填充方式 | 按钮 + 自动 | 仅按钮 |
| 延时设置 | 支持 | 移除 |
| 通知提示 | 支持 | 移除 |
| 设置选项 | 复杂 | 简化 |
| 响应速度 | 有延迟 | 即时 |
| 用户体验 | 复杂 | 简洁 |

## 🎨 界面变化

### 设置标签页
- **移除**: 自动填充开关、通知设置、延时设置
- **保留**: 数据管理、使用说明
- **新增**: 简洁的使用步骤说明

### 填充按钮
- **样式**: 保持橙色渐变设计
- **功能**: 点击即填充，无延迟
- **反馈**: 仅保留输入框高亮效果

## 🚀 性能提升

1. **启动速度**: 减少了设置加载时间
2. **填充速度**: 移除了延时，填充更快速
3. **内存占用**: 减少了不必要的功能代码
4. **响应性**: 用户操作更直接，无等待

## 📝 使用建议

1. **数据准备**: 建议提前在插件中设置好所有价格数据
2. **批量导入**: 使用CSV或JSON格式批量导入数据更高效
3. **货号匹配**: 确保货号完全匹配（区分大小写）
4. **测试验证**: 在测试页面验证数据正确性

## 🔍 故障排除

### 常见问题
1. **按钮不显示**: 刷新页面或重新加载插件
2. **价格不填充**: 检查货号是否完全匹配
3. **数据丢失**: 使用导入导出功能备份数据

### 调试方法
1. 按F12打开开发者工具
2. 查看Console标签页的调试信息
3. 使用测试页面验证功能

## 📈 未来扩展

插件现在更加专注于核心功能，为未来扩展留下了空间：
- 可以添加更多数据源支持
- 可以增加批量操作功能
- 可以优化匹配算法
- 可以添加数据验证功能

优化后的插件更加简洁高效，专注于解决核心问题！

