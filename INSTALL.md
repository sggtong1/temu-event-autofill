# 安装和使用指南

## 快速开始

### 1. 准备图标文件
由于图标文件需要手动生成，请按以下步骤操作：

1. 打开 `create-icons.html` 文件（在浏览器中打开）
2. 点击"下载16x16"、"下载48x48"、"下载128x128"按钮
3. 将下载的文件重命名为：
   - `icon16.png`
   - `icon48.png` 
   - `icon128.png`
4. 将这三个文件放入 `icons` 文件夹中

### 2. 安装插件
1. 打开Chrome浏览器
2. 访问 `chrome://extensions/`
3. 开启右上角的"开发者模式"开关
4. 点击"加载已解压的扩展程序"
5. 选择本插件文件夹（包含manifest.json的文件夹）
6. 插件安装完成！

### 3. 首次使用
1. 点击浏览器工具栏中的插件图标
2. 插件会自动加载示例数据
3. 你可以：
   - 查看现有的价格数据
   - 添加新的价格数据
   - 导入你的CSV或JSON数据文件

### 4. 在Temu页面使用
1. 访问Temu活动页面：
   `https://agentseller.temu.com/activity/marketing-activity/detail-new`
2. 页面加载完成后，你会看到"💰 自动填充活动价格"按钮
3. 点击按钮即可自动填充所有匹配的价格

## 数据导入示例

### 导入CSV文件
创建一个CSV文件，格式如下：
```csv
货号,活动价格,成本价格,备注
92-BK-15,48.24,30.00,便携式水冷风扇-黑色
84-GR-54,48.24,30.00,便携式水冷风扇-灰色
```

### 导入JSON文件
创建一个JSON文件，格式如下：
```json
[
  {
    "itemNumber": "92-BK-15",
    "activityPrice": 48.24,
    "costPrice": 30.00,
    "note": "便携式水冷风扇-黑色"
  }
]
```

## 常见问题

### Q: 插件图标不显示？
A: 请确保已按照步骤1生成了图标文件并放入icons文件夹。

### Q: 在Temu页面看不到填充按钮？
A: 请确保：
- 在正确的Temu活动页面
- 页面已完全加载
- 刷新页面重试

### Q: 价格没有自动填充？
A: 请检查：
- 货号是否完全匹配（区分大小写）
- 价格数据是否正确设置
- 查看浏览器控制台是否有错误信息

### Q: 如何更新插件？
A: 在扩展程序页面点击"重新加载"按钮即可。

## 技术支持

如果遇到问题，请：
1. 按F12打开开发者工具
2. 查看Console标签页的错误信息
3. 检查插件是否正确安装
4. 确认在正确的Temu页面使用

## 更新日志

### v1.0.0 (2024-01-01)
- 初始版本发布
- 支持自动价格填充
- 支持数据管理
- 支持导入导出功能

